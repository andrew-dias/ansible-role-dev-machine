---
### Variables for included roles must go here ###

# geerlinguy.docker
docker_users:
  - "{{ ansible_user_id }}"

# ansible-gnome-extensions
gnome_extension_ids:
  - 615 # AppIndicator support
  - 750 # OpenWeather
  - 302 # windowoverlay-icons

# ansible-role-vagrant
vagrant_home: "{{ ansible_env.HOME }}/.local/share/vagrant"
